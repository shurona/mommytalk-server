# local은 포트는 다르게 설정
server:
  port: 19100
  servlet:
    session:
      tracking-modes: cookie

spring:
  jpa:
    hibernate:
      ddl-auto: update
  task:
    scheduling:
      enabled: false  # local에서는 스케줄러 비활성화
