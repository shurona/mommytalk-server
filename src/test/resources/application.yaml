# DataSource
spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  # JPA/Hibernate
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true;